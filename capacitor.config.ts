import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raviteja.traveltracker',
  appName: 'travel-tracker',
  webDir: 'build'
};

export default config;
